# --- Cấu hình Server ---
server.port=8080

# --- Cấu hình Database (MySQL) ---
# Thay 'hotel_db' bằng tên database bạn đã tạo trong MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/hotel_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Tên đăng nhập (thường là root)
spring.datasource.username=root

# --- QUAN TRỌNG: Bảo mật mật khẩu ---
spring.datasource.password=<YOUR_DB_PASSWORD>

# --- Cấu hình JPA / Hibernate ---
# update: Tự động tạo/cập nhật bảng khi chạy code (tốt cho Dev)
# validate: Chỉ kiểm tra, không sửa DB (tốt cho Prod)
spring.jpa.hibernate.ddl-auto=update

# Hiển thị câu lệnh SQL trong Console để dễ debug
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect